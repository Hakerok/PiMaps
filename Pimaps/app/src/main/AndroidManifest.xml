<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pimaps"
    android:versionCode="1"
    android:versionName="1.0"
    android:installLocation="preferExternal">

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconka"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".PiMAPS"
            android:label="@string/app_name" >
        </activity>
        <activity android:name=".SplashScreenActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".o_programme"
            android:label="@string/o_programme">
             </activity>
        <activity android:name=".napisat"
            android:label="@string/napisat">

            </activity>
        <activity android:name=".one"
            android:label="@string/one">

            </activity>
        <activity android:name=".zero"
            android:label="@string/zero">

            </activity>
        <activity android:name=".two"
            android:label="@string/two">

            </activity>
        <activity android:name=".three"
            android:label="@string/three">

            </activity>
    </application>

</manifest>
